import 'package:flutter/material.dart';

class DetailsPagePokemonStart extends StatelessWidget {
  const DetailsPagePokemonStart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}